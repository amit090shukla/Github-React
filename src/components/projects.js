import React from 'react';

const Project = (props) => {
    console.log('here');
    return (
        <div>
            {console.log(props)}
        </div>
    )
}

export default Project;
